name: build
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    steps:
      - name: Print environment secret [1]
        run: echo ${{ env.SNYK_TOKEN }}


